<%= form_for(search_image) do |f| %>
  <% if search_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(search_image.errors.count, "error") %> prohibited this search_image from being saved:</h2>

      <ul>
      <% search_image.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
